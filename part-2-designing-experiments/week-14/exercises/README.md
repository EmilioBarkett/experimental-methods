# Week 14 exercises

Suggested starting point:

- Reproduce a small causal-forest / heterogeneous-treatment-effects example (e.g. `grf` in R) on simulated data.

Not every week needs code — some are discussion/design-only (see notes.md).
Add `.R` / `.py` / `.md` files here as you work through them.
